# Cosenseリンク肥大化警告 — 実装方針

## 目的

Cosense（旧Scrapbox）で、多数のページから参照されているリンクを検出し、Discordへ警告する。

## 確定事項

- ケイはRender上で動く既存のDiscord Bot（Python）である。
- Renderの利用は継続する。
- 外部から定期アクセスがあるため、Renderのスリープは考慮しない。
- 監視処理は既存Botの定期タスクとして動かす。
- 警告対象外のリンクは、Cosense内の専用設定ページに記載する。
- 通知先は既存の警告と同じDiscordチャンネルを使用する。
- 一度警告したリンクは、リンク数が十分減るまで再通知しない。

## 動作

1. 30分ごとにCosenseのページ一覧を取得する。
2. 各ページの被リンク数（APIの `linked`）を確認する。
3. 設定ページに記載されたリンクを除外する。
4. 未警告かつ30以上ならDiscordへ通知する。
5. 警告後は、25以下になるまで再通知しない。

```text
30以上 → 1回だけ警告
26〜29 → 現在の状態を維持
25以下 → 警告状態を解除
```

閾値と監視間隔は環境変数で変更可能にする。

## データ保存

警告状態はBotプロセスのメモリ内で管理し、永続化しない。

保持する項目：

- CosenseのページID
- ページタイトル
- 警告中かどうか
- 最後に確認した被リンク数
- 警告日時
- 初回走査済みかどうか

再デプロイや再起動後に状態が失われることは考慮しない。

## 初回起動

導入時点ですでに閾値を超えているページを一斉通知しない。

初回はメモリに状態を登録するだけにし、以後新しく閾値を超えたページを通知する。

## 実装前に確認すること

Cosense APIの `linked` が「そのページを参照しているページ数」と一致するか、既知のページで確認する。

一致しない場合のみ、更新ページの本文からリンクを抽出して独自集計する方式へ変更する。

## 想定する主な変更

- Cosenseページ一覧の取得処理
- 除外設定ページの読み取り処理
- 警告判定とDiscord通知
- メモリ上での警告状態管理
- 既存Botへの30分間隔タスク追加

## 未決事項

- 除外設定ページの名前（後から決める）

この方針に基づく実装を追加済み。
